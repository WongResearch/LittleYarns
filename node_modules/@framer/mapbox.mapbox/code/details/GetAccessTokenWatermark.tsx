import * as React from 'react'
import { MapboxControlComponent, Props as ControlProps } from './MapboxControlComponent'
import { InteractiveMap } from 'react-map-gl'

interface Props {
}

export const GetAccessTokenWatermark = (props: Props) => {
    return (
        <a style={overlayStyle} href="https://account.mapbox.com/access-tokens/?pluginName=FramerX">
            Get your access token
        </a>
    )
}

GetAccessTokenWatermark.defaultProps = {
}

const overlayStyle: React.CSSProperties = {
    position: "absolute",
    alignSelf: "flex-start",
    fontSize: "1.5em"
}
